name: Reusable Build and Test

on:
  workflow_call:
    inputs:
      branch:
        required: false
        type: string
        default: ""
      ref:
        description: "Git ref (commit SHA, branch, or tag) to checkout. Defaults to the triggering ref."
        required: false
        type: string
        default: ""
    outputs:
      artifact-id:
        description: "The ID of the uploaded artifact"
        value: ${{ jobs.build-and-test.outputs.artifact-id }}
      sha:
        description: "The short SHA of the commit"
        value: ${{ jobs.build-and-test.outputs.sha }}

permissions:
  contents: read

env:
  BUILD_ARTIFACT_NAME: build-artifact
  PROXY_ARTIFACT_NAME: proxy-artifact

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    outputs:
      artifact-id: ${{ steps.upload.outputs.artifact-id }}
      sha: ${{ steps.short-sha.outputs.sha }}

    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ inputs.ref || github.ref }}

      - uses: benjlevesque/short-sha@v3.0
        id: short-sha

      - uses: pnpm/action-setup@v4
        with:
          version: latest
          run_install: false

      - uses: actions/setup-node@v6
        with:
          node-version-file: ".nvmrc"
          cache: "pnpm"

      - run: pnpm install
        env:
          CI: true

      - run: pnpm -w build

      # - run: pnpm run lint
      #   continue-on-error: false

      - run: pnpm -w test

      # - run: pnpm run test:coverage
      #   continue-on-error: true

      - run: pnpm -w validate

      - name: Build with metadata
        run: |
          BUILD_INFO='window.cps_global_components_build = {Sha: "${{ steps.short-sha.outputs.sha }}", RunId: "${{ github.run_number }}", Timestamp: "${{ github.event.repository.updated_at }}"'
          if [ -n "${{ inputs.branch }}" ]; then
            BUILD_INFO="${BUILD_INFO}, Branch: \"${{ inputs.branch }}\""
          fi
          BUILD_INFO="${BUILD_INFO}};"
          pnpm --filter cps-global-components run rollup --intro "$BUILD_INFO"

      # Browser auto-download is disabled workspace-wide via .puppeteerrc.cjs
      # (skipDownload) to stop the two concurrent puppeteer postinstalls racing on
      # the shared cache. e2e needs a real Chrome, so install it here as one
      # serial step. PUPPETEER_SKIP_DOWNLOAD=false (a step-level env, which is
      # reliably exported to the shell — unlike an inline prefix in a pnpm script)
      # overrides skipDownload so the install actually downloads. Verify before
      # running the tests so any failure is unambiguous.
      # Puppeteer's own browser downloader is unreliable on this runner: the
      # setup-node pnpm cache is restored with `tar -P` (absolute paths) which
      # recreates a partial ~/.cache/puppeteer, and a fresh `puppeteer browsers
      # install` downloads the archive but leaves it un-extracted (folder present,
      # executable missing). Rather than fight it, use the Chrome that ships on
      # the GitHub ubuntu runner via PUPPETEER_EXECUTABLE_PATH — no download, no
      # extract, no cache races. Setting that var also makes puppeteer skip any
      # download. (.puppeteerrc skipDownload already stops the install-time race.)
      - name: Run e2e tests against the runner's system Chrome
        run: |
          cp -r ./packages/cps-global-components/dist/global-components.js ./e2e/harness
          CHROME_BIN="$(command -v google-chrome || command -v google-chrome-stable || command -v chromium-browser || command -v chromium || true)"
          echo "System Chrome: ${CHROME_BIN:-<none found>}"
          [ -n "$CHROME_BIN" ] || { echo "No system Chrome on the runner; cannot run e2e tests"; exit 1; }
          export PUPPETEER_EXECUTABLE_PATH="$CHROME_BIN"
          pnpm --filter e2e test

      - run: |
          rm -rf ./to-deploy
          mkdir -p ./to-deploy
          cp ./packages/cps-global-components/dist/global-components.js ./to-deploy
          cp ./packages/cps-global-components/dist/global-components.js.map ./to-deploy
          cp ./packages/cps-global-script-redirect/dist/cps-global-components.js ./to-deploy
          cp ./packages/cps-global-handover/auth-handover.html ./to-deploy
          cp ./packages/cps-global-handover/dist/auth-handover.js ./to-deploy
          cp ./packages/cps-global-handover/dist/auth-handover.js.map ./to-deploy
          cp -r ./packages/cps-global-preview/dist ./to-deploy/preview
          cp -r ./packages/cps-global-accessibility/dist ./to-deploy/accessibility

      - uses: actions/upload-artifact@v7
        id: upload
        with:
          name: ${{env.BUILD_ARTIFACT_NAME}}
          path: |
            ./to-deploy/*
          retention-days: 1

      - uses: actions/upload-artifact@v7
        id: upload-proxy
        with:
          name: ${{env.PROXY_ARTIFACT_NAME}}
          path: |
            ./infra/proxy/dist/*
          retention-days: 1
