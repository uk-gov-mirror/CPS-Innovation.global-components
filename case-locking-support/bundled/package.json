{
  "name": "case-locking-support-bundled",
  "version": "0.0.1",
  "private": true,
  "description": "Bundles @microsoft/signalr + the case-locking PoC script into a single browser IIFE.",
  "type": "module",
  "scripts": {
    "build": "esbuild src/main.js --bundle --minify --format=iife --target=es2018 --outfile=dist/main.js"
  },
  "dependencies": {
    "@microsoft/signalr": "^8.0.7"
  },
  "devDependencies": {
    "esbuild": "^0.28.0"
  }
}
